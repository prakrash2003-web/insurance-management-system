# Core application
Django==4.2.16
django-environ==0.11.2
django-widget-tweaks==1.5.0
Pillow==10.4.0

# Static file serving (also used in local dev via finders)
whitenoise==6.7.0

# Production WSGI server (installs locally too; only run in production)
gunicorn==23.0.0

# PostgreSQL driver - production only. Skipped on Windows, where local
# development uses SQLite and no prebuilt wheel exists for Python 3.8.
psycopg2-binary==2.9.10 ; sys_platform != "win32"
